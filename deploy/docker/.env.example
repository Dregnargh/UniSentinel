# Copy to .env next to compose.yaml.
POSTGRES_PASSWORD=change-me
# Session signing secret — generate with: openssl rand -base64 32
AUTH_SECRET=change-me-too
WEB_PORT=8080
LOG_LEVEL=info
# IMAGE=unisentinel/grc
# TAG=latest
