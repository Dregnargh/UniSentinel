node_modules/
dist/

# design-sync staging / build output / machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# misc
*.log
.DS_Store
.brandtmp/
.wf/

# npm workspaces: the single lockfile lives at the repo root and IS committed.
# (Both Next apps pin outputFileTracingRoot to the repo root so tracing stays correct.)
.next/
*.tsbuildinfo
