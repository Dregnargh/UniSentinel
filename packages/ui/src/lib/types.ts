/** Shared scales used across UniSentinel components. */

/** Control size scale. `md` is the default for every sized component. */
export type Size = 'sm' | 'md' | 'lg';

/**
 * Semantic status tone, used by Badge, Tag, Alert, Progress and friends to map
 * onto the design system's semantic color tokens.
 */
export type Tone = 'neutral' | 'brand' | 'info' | 'success' | 'warning' | 'danger';
